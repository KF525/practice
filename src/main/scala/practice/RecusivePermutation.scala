package practice

class RecusivePermutation {
/*  def getPermutations(string: String) = {
    buildCombinations()
  }

  def buildCombinations(currentString: String,
                        remainingLetters: List[String]): String =
    remainingLetters match {
      case h::t =>
      case _ =>

    }*/
}
//a => at/ta =>

//val (grp1, grp2) = string.splitAt(number)
//        buildCombinations(currentLetter, grp1 + currentLetter + grp2, number-1)
//abc acb bac bca cba cab
//ca ac
//tca cta cat tac atc act